import java.awt.*;
import java.awt.event.*;

public class ImageTextAWT extends Frame {

    Image img;

    public ImageTextAWT() {
        setTitle("AWT Image and Text Display");
        setSize(500, 400);
        setLayout(null);

        // Load image (make sure image file is in project folder)
        img = Toolkit.getDefaultToolkit().getImage("sample.jpg");

        // Close window event
        addWindowListener(new WindowAdapter() {
            public void windowClosing(WindowEvent e) {
                dispose();
                System.exit(0);
            }
        });

        setVisible(true);
    }

    public void paint(Graphics g) {
        // Display text
        g.setFont(new Font("Arial", Font.BOLD, 20));
        g.drawString("Welcome to AWT Image Viewer", 100, 80);

        // Display image
        g.drawImage(img, 150, 120, 200, 200, this);
    }

    public static void main(String[] args) {
        new ImageTextAWT();
    }
}
